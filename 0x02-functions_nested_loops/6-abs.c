#include "main.h"
/**
 *_abs - Returns the absolute value of an int 
 *@nu: integer to make absolute
 *Return: Abosulte numbers
 */
int _abs(int nu)
{
	if (nu > 0)
	{
		return (nu);
	}
	else
		return (nu * -1);
}
